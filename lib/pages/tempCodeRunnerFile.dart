/*
  bool isLoggedIn = AuthenticationProvider.isLoggedIn.value;
  // Use the isLoggedIn value to conditionally render different content
  if (isLoggedIn) {
    print('User is logged in ${AuthenticationProvider.userId}, ${AuthenticationProvider.userId?.isEmpty}');
  } else {
    print('User is not logged in');
  }
*/
